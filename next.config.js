/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

const nextLangs = {
  i18n: {
    locales: ["fr-FR"],
    defaultLocale: "fr-FR",
  },
};

module.exports = nextConfig;
module.exports = nextLangs;
