/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

const nextLangs = {
  i18n: {
    locales: ["fr", "en"],
    defaultLocale: "fr",
  },
};

module.exports = nextConfig;
module.exports = nextLangs;
